# JpgLossless v1.1 更新：反馈通道搬家 + 界面微调

JpgLossless 是个纯本地运行的图片压缩工具——双击 exe 就用，图片全程不上传任何服务器，主打「无损压到最小」。

这次 v1.1 主要更新两点：

1. **反馈通道迁移**：作者 GitHub 账号从 `wenzongze` 改名为 `neutron-star77`（旧的已弃用）。现在软件里点「反馈」提交的 Issue 直接进新仓库 `neutron-star77/jpg-lossless`；配套的解密页也搬到了 `neutron-star77.github.io/beast_decode_page/`。
2. **压缩质量输入框收窄**：之前那个框继承了全局样式偏宽，现在改成正好 4 位数字宽度，界面更紧凑了。

核心能力没变：原格式无损压缩（JPG/PNG）、转 WebP / PNG / JPG、输出上限 + 压缩质量可调、批量拖入、每行像素对比条、断点续压、明暗双主题。

下载：GitHub Releases 搜 `neutron-star77/jpg-lossless`，下 `JpgLossless.exe` 双击即用（缺 WebView2 就装一下运行时）。
