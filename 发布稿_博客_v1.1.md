# JpgLossless v1.1：本地图片无损压缩工作台

> 纯本地、不上传、双击即用的一款图片压缩工具。本文介绍它的核心能力与亮点，并说明 v1.1 的更新。

## 它是什么

JpgLossless 是一个跑在你自己电脑上的图片压缩工作台。界面用系统 Edge WebView2 内核渲染（工程蓝图风格），压缩逻辑全部在本机完成，**图片不会上传任何服务器**——这一点对隐私和批量处理大图都很重要。

打包后是单文件 `JpgLossless.exe`，双击即用，不用装 Python、不用配环境。

## 核心功能

- **原格式无损压缩**：保持原图格式（JPG / PNG），压到该格式能压到的最小体积。这是它的核心卖点——不是「转格式换小」，而是「同格式压到最小且画质不变」。
- **转格式输出**：支持转 WebP（无损 / 有损）、PNG（无损）、JPG（有损，质量可调）。
- **输出上限 + 压缩质量**：上限只对「转 JPG / WebP / PNG」这类有损输出生效——超过上限会自动逐级降质直到达标；PNG 无损无法按质量缩小，超限会自动转 JPG 达标。压缩质量拉到 100 时转这些格式也是无损。原格式无损则忽略上限。
- **批量与目录**：支持拖入多文件、文件夹递归、保持目录结构；可自动按 001 / 002… 重命名、加文件名后缀、多线程并行。
- **预览与对比**：每个文件行里有一条像素对比条（灰色=原大小，蓝/绿=压缩后，变绿=更小，变橙=变大），配合等宽字体的字节数与百分比，结果一眼可见；还可「预览选中对比」先看效果再压。
- **省心细节**：断点续压（resume）、压缩完自动打开输出文件夹、可选自动删除原图；所有偏好写入 exe 同目录的 `config.json`，下次启动自动恢复。
- **一键反馈**：内置反馈按钮，预填 GitHub Issue 直接提交给作者（仓库固定，不可改），方便报 bug 或提建议。

## 亮点

- **隐私优先**：纯本地运算，图片不出本机。
- **新拟物派 UI + 工程蓝图顶部**：界面走 Neumorphism 柔和凸起风，顶栏是普鲁士蓝 + 蓝图网格，明暗双主题可切。
- **压缩引擎强**：默认用 ECT（当前最强的 PNG/JPG 无损压缩器之一），另支持 jpegtran、jpegoptim；原格式无损模式若本地缺引擎会自动联网拉取。
- **轻量单文件**：PyInstaller 打包，自带引擎，不依赖外部文件。

## v1.1 更新了什么

1. **GitHub 账号体系迁移**：作者账号由 `wenzongze` 更名为 `neutron-star77`（`hewll` 被占用）。反馈通道（Issue）现指向 `neutron-star77/jpg-lossless`；兽语解密页迁移到 `neutron-star77.github.io/beast_decode_page/`。
2. **压缩质量输入框收窄**：把「压缩质量」右侧的文本框从过宽（继承全局 150px）收紧为正好 4 位数字宽度，界面更紧凑。
3. **工程侧调整**：git 提交邮箱改用 GitHub noreply 地址，避免开启「暴露邮箱即拦截推送」后推送失败（用户无感，属发版配套）。

## 仓库与下载

- GitHub：https://github.com/neutron-star77/jpg-lossless
- Gitee：https://gitee.com/cloud_moon_yarn/jpg-lossless
- 下载：进仓库的 Releases / 发行版，找最新 `v1.1`，在 Assets 里下 `JpgLossless.exe`（首次双击若提示缺 WebView2，装一下「WebView2 Runtime」即可）。
