# 从 0 写一个纯本地的图片无损压缩工具：JpgLossless 开发全记录

> 关键词：本地优先 · 图片无损压缩 · WebView2 · pywebview · ect · 单文件 exe
> 仓库：GitHub https://github.com/wenzongze/jpg-lossless ｜ Gitee https://gitee.com/cloud_moon_yarn/jpg-lossless

---

## 一、为什么要自己写一个

网上压缩图片的方案无非两类：

1. **在线网站**：永远绕不开「上传」二字。原图传到别人的服务器，隐私没了；默认有损，画质肉眼可见地糊；批量一多就限速、要会员。
2. **本地 GUI 工具**：要么界面古早（十年前的 WinForms 既视感），要么只做有损，要么不支持中文路径。

我想要的是：**图不出本机、原格式真无损、界面能看、还能批量拖文件夹**。没有现成的，就自己写一个——这就是 JpgLossless。

核心承诺只有一句：**所有压缩都在你电脑上完成，图片不会上传任何服务器。**

---

## 二、技术栈（详细）

| 层 | 技术 / 库 | 作用 | 备注 |
|----|-----------|------|------|
| 语言 | **Python 3.x** | 后端主逻辑 | 跨平台、生态成熟 |
| 压缩引擎 | **Efficient Compression Tool (ect)** | JPG/PNG 原格式无损压缩 | C++ 实现，霍夫曼优化 + 元数据清理，真正无损 |
| 转格式 | **Pillow (PIL)** + libwebp | JPG→WebP/PNG/JPG 转码 | WebP 质量 100 即无损 |
| 界面框架 | **pywebview** | 用系统原生浏览器内核承载前端 | 非 Electron，体积小、零 Chromium 打包 |
| 渲染内核 | **Microsoft Edge WebView2** | 实际渲染 HTML/CSS/JS 界面 | Windows 系统通常自带运行时 |
| 前端 | 原生 **HTML + CSS + Vanilla JS** | 工作台 UI | 四套主题靠 CSS 作用域类切换 |
| 打包 | **PyInstaller** (`--onefile --noconsole`) | 打成单文件 exe | 双击即用，无需装 Python |
| 通信 | pywebview 的 JS↔Python 桥 | 前后端事件同步 | 后端只塞事件进线程安全队列，前端轮询拉取 |

### 为什么是 WebView2 而不是 tkinter / Electron

- **tkinter**：高 DPI 屏下默认位图拉伸、文字发虚，做现代 UI 很吃力。
- **Electron**：把整个 Chromium 打进包，体积上百 MB，纯本地小工具没必要。
- **pywebview + WebView2**：复用系统已有的 Edge 内核，包体小、渲染清晰，还能写出现代 CSS（新拟物 / 工程蓝图风）。代价是 Windows 专属 + 要处理好 WebView2 的线程模型。

---

## 三、架构与数据流

```
[拖入文件/文件夹]
      │
      ▼
[前端 JS 收集] ──bridge──> [Python 后端]
                              │
                  ┌───────────┼──────────────┐
                  ▼           ▼              ▼
            [ect 无损压缩]  [Pillow 转格式]  [ASCII 中转处理中文路径]
                  │           │              │
                  └───────────┼──────────────┘
                              ▼
              [按设置写盘：保持目录结构 / 重命名]
                              │
                  [事件入线程安全队列]
                              │
                  ┌───────────┴───────────┐
                  ▼                       ▼
        [前端轮询拉取进度]      [像素对比条 / 字节数 / 百分比 实时刷新]
```

后端永不主动 `evaluate_js` 注入前端（否则 WebView2 在 Windows 下会死锁），而是把状态变更放进线程安全队列，前端用定时器轮询——这是整个稳定性设计的关键。

---

## 四、开发里踩过的坑（重点）

### 1. 中文 / 全角括号路径崩溃
`ect` 用 `std::filesystem` 打开文件，Windows 上遇到中文或全角括号路径会报 `encoding error 6`，直接崩。

**解法**：先把待处理文件复制到临时 **ASCII 名** 路径让 ect 跑，处理完再拷回原路径。中文目录名从此随便用。

### 2. WebView2 死锁（最阴间的一个）
一开始后台压缩线程直接调 `evaluate_js` 把结果推给前端，Windows 下表现为「点开始就卡死」。

**解法**：后端只往线程安全队列塞事件，前端轮询拉取；再加一个**看门狗**定时检测，卡死的按钮自动恢复可用。

### 3. 大批量卡死
几千张图一次性 base64 拼成巨大数组跨桥传输，直接撑爆 pywebview 的桥接层。

**解法**：
- 分批传输（每批约 24MB 原始数据）；
- 缩略图**懒加载**（滚动到才生成）；
- 多线程并行压缩，2GB 量级也能稳稳跑完。

### 4. 单文件 exe 的持久化
PyInstaller `--onefile` 运行时 `__file__` 指向临时解压目录，配置写那会丢失，重启即还原。

**解法**：统一把 `config.json`、压缩引擎 `ect.exe` 落到 **exe 所在目录**，跨重启保留用户设置。

### 5. 对比条（体积可视化）
每个文件行下方两条细条：上条灰=原大小（100% 基准），下条彩色=压缩后大小，宽度 ∝ `new/old`，**绿=压小、橙=变大**；条下还有 `原字节 → 新字节  -xx.x%` 文字。压缩结果一眼可见，不用猜。

---

## 五、现在能做什么

- ✅ 原格式无损（JPG / PNG，依赖 `ect`）
- ✅ 转 WebP（质量 100 即无损）/ PNG / JPG（有损，质量可调）
- ✅ 批量拖入、文件夹递归、保持原目录结构
- ✅ 按数字批量重命名（001、002…）、输出体积上限
- ✅ 每条文件带体积对比条 + 字节数 + 百分比
- ✅ 输出前「预览选中对比」、处理完自动打开输出文件夹
- ✅ 失败图片跨重启持久化、可一键重试
- ✅ 四套主题（经典 / 新拟物 × 日 / 夜），设置自动记忆

---

## 六、快速使用

1. 双击 `JpgLossless.exe`（无需安装 Python）。
2. 把图片 / 文件夹拖进右侧「拖入区」，或点「选择文件 / 选择文件夹」。
3. 左侧设置保存位置、目标格式、后缀、质量等。
4. 选中文件点「预览选中对比」查看前后效果。
5. 点「开始压缩」。

> 运行环境：Windows 10 / 11，需 Edge WebView2 运行时（系统通常自带）。压缩引擎 `bin/ect.exe` 已随包内置；原格式无损模式缺失时自动联网下载。

---

## 七、下载与安装（推荐从 Release 页面获取）

> 仓库里 Clone / 下载的是「源码」，并不能直接双击运行。最省事的方式是到 Release 页面下载已经打包好的单文件 exe。

### 为什么从 Release 下载，而不是 Clone 源码？

- **Clone / 下载源码**：拿到的是 Python 源代码（`.py`、前端 HTML 等），没有打包成可执行程序，双击打不开；要自己装 Python 3.x、执行 `pip install pywebview pillow`，再用 PyInstaller 打包，对普通用户门槛很高。
- **Release 页面 = 官方发布的成品**：开发者已把源码连同压缩引擎 `ect.exe` 一起打包成单文件 `JpgLossless.exe`，**双击即用，无需安装任何环境**，版本号清晰，出问题能对照版本反馈。
- 每次发版都在 Release 里附上对应版本的 exe 与更新说明，下载到的就是当前最稳定、已验证的版本。

### 需要下载什么？

1. **`JpgLossless.exe`**（必需）：在仓库的 **Releases** 里下载——
   - GitHub：进仓库主页，点右侧 **Releases**（或「发布」），找到最新版本（如 `v1.0`），在 `Assets` 里点 `JpgLossless.exe`。
   - Gitee：进仓库主页，点 **发行版**，同样找到最新版本下载 `JpgLossless.exe`。
   - 单文件，已内置压缩引擎，不依赖仓库里的 `bin/ect.exe` 目录。
2. **Microsoft Edge WebView2 运行时**（仅在双击 exe 报错时按需安装）：Windows 10/11 通常已自带；若双击后提示缺少 WebView2，到微软官网下载安装「WebView2 Runtime」（Evergreen Bootstrapper）即可。

> 不需要下载源码、不需要装 Python、不需要手动放 `ect.exe`。

### 下载之后怎么用？（详细步骤）

1. **下载**：进仓库 Releases 页面，找到**最新版本**，在「Assets / 资源」里点 `JpgLossless.exe` 下载到本地。
2. **（可选）放好位置**：挪到习惯目录（如 `D:\Tools\`），单文件不挑位置，可建桌面快捷方式。
3. **双击运行**：直接双击 `JpgLossless.exe`。正常即弹窗进入工作台；若弹窗报错「无法启动，因为计算机中丢失 …」或提示缺少 WebView2，按上安装 WebView2 Runtime 后再双击。
4. **开始压缩**（同「快速使用」）：拖入图片/文件夹 → 设置输出 → 预览对比 → 点「开始压缩」。
5. **设置自动保存**：偏好写入 exe 同目录 `config.json`，下次启动自动恢复。

## 八、界面截图

主界面（深色「工程蓝图」风，带体积对比条）：

![主界面 · 带体积对比条](screenshots/main.png)

设置面板（主题 / 引擎 / 压缩等级）：

![设置面板 · 主题 / 引擎 / 等级](screenshots/settings.png)

---

## 九、写在最后

工具已经打包成双击即用的单文件 exe，源码也在整理中。如果你也在意图片隐私和画质，欢迎试试，也欢迎提需求、提 Issue。

**项目地址（双端仓库）：**
- GitHub：https://github.com/wenzongze/jpg-lossless
- Gitee：https://gitee.com/cloud_moon_yarn/jpg-lossless

---

*如果你觉得这篇对你有启发，欢迎转发；转载请保留仓库链接。*
